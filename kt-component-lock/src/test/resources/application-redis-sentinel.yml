spring:
  redis:
    host: localhost
    port: 6379
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 8
        max-wait: -1ms
        time-between-eviction-runs: 1
    sentinel:
      master: mymaster
      nodes:
        - localhost:26379