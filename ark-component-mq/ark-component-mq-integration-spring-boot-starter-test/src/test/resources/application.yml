ark:
  component:
    mq:
      rocketmq:
        server: localhost:9876
        producer:
          group: 'pg_default'
      rabbitmq:
        server: localhost:5672
logging:
  level:
    root: info